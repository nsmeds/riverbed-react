import React from 'react';

const Contributor = props => {
    // console.log('hi from Contributor, here is props.name: ', props.name);
    <li>
        {props.name}
    </li>
}

export default Contributor;